<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/header_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/header_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_layout_height"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_gravity="center_horizontal"
        android:background="@color/white"
        android:clickable="true"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/menu_item"
            android:layout_width="@dimen/header_menu_icon_width_height"
            android:layout_height="@dimen/header_menu_icon_width_height"
            android:layout_gravity="center"
            android:layout_marginLeft="@dimen/header_menu_icon_top_margin"
            android:layout_marginStart="@dimen/header_menu_icon_top_margin"
            android:layout_marginTop="@dimen/header_menu_icon_top_margin"
            android:src="@drawable/menu_icon" />

        <ImageView
            android:id="@+id/logo_for_feed"
            android:layout_width="@dimen/header_logo_width_height"
            android:layout_height="@dimen/header_logo_width_height"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentTop="true"
            android:layout_gravity="end"
            android:layout_marginBottom="@dimen/header_logo_margin"
            android:layout_marginTop="@dimen/header_logo_margin"
            android:src="@drawable/logo_for_header" />
    </RelativeLayout>

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/header_avatar"
        android:layout_width="@dimen/header_avatar_width_height"
        android:layout_height="@dimen/header_avatar_width_height"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/header_avatar_margin_top"
        android:gravity="center_horizontal"
        android:src="@drawable/avatar_with_photo" />

</RelativeLayout>