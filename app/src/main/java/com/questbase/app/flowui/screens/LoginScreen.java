package com.questbase.app.flowui.screens;

public class LoginScreen implements RespoScreen  {
    @Override
    public boolean equals(Object o) {
        return o != null && o instanceof LoginScreen;
    }
}
