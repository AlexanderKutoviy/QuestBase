package com.questbase.app.flowui.screens;

public class InternetWarningScreen implements RespoScreen {
    @Override
    public boolean equals(Object o) {
        return o != null && o instanceof InternetWarningScreen;
    }
}