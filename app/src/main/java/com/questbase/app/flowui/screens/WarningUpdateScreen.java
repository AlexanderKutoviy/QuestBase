package com.questbase.app.flowui.screens;

public class WarningUpdateScreen implements RespoScreen {
    @Override
    public boolean equals(Object o) {
        return o != null && o instanceof WarningUpdateScreen;
    }
}
